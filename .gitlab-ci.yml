include:
  - template: Auto-DevOps.gitlab-ci.yml

test:
  image: python:3.8-buster
  before_script:
    - pip install -r requirements.docs.txt
  stage: test
  script:
  - mkdocs build --strict --verbose --site-dir test
  artifacts:
    paths:
    - test

pages:
  image: python:3.8-buster
  before_script:
    - pip install -r requirements.docs.txt
  stage: deploy
  script:
    - mkdocs build --strict --verbose
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME == $CI_DEFAULT_BRANCH'
    - changes:
        - docs/*

build:
  stage: build
  image: lthn/build
  variables:
    DOCKER_HOST: tcp://docker:2375
  services:
    - name: docker:dind
      command: [ "--experimental" ]
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - docker build -t lthn/build:latest -f build-conf/build.Dockerfile src
    - docker push lthn/build:latest
  rules:
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME == $CI_DEFAULT_BRANCH'
    - changes:
        - build-conf/build.Dockerfile


chain.linux:
  stage: build
  image: docker
  services:
    - name: docker:dind
      command: [ "--experimental" ]

  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - docker build --squash -t lthn/build:chain -f build-conf/chain/linux.Dockerfile src
    - docker push "lthn/build:chain"
  rules:
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME == $CI_DEFAULT_BRANCH'
    - changes:
        - build-conf/chain/linux.Dockerfile

wallet.linux:
  stage: build
  image: docker
  services:
    - name: docker:dind
      command: [ "--experimental" ]

  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - docker build --squash -t lthn/build:wallet-linux -f build-conf/wallet/linux.Dockerfile src
    - docker push "lthn/build:wallet-linux"
  rules:
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME == $CI_DEFAULT_BRANCH'
    - changes:
        - build-conf/wallet/linux.Dockerfile

wallet.windows:
  stage: build
  image: docker
  services:
    - name: docker:dind
      command: [ "--experimental" ]

  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - docker build --squash -t lthn/build:wallet-windows -f build-conf/wallet/windows.Dockerfile src
    - docker push "lthn/build:wallet-windows"
  rules:
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME == $CI_DEFAULT_BRANCH'
    - changes:
        - build-conf/wallet/windows.Dockerfile

wallet.android:
  stage: build
  image: docker
  services:
    - name: docker:dind
      command: [ "--experimental" ]

  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - docker build --squash -t lthn/build:wallet-android -f build-conf/wallet/android.Dockerfile src
    - docker push "lthn/build:wallet-android"
  rules:
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME == $CI_DEFAULT_BRANCH'
    - changes:
        - build-conf/wallet/android.Dockerfile
